<script lang="ts">
import {ref} from "vue"

const inputTextarea = ref("テキストエリアへの入力文字。/n改行も加えてみる。")
const memberType = ref(1)
const memberTypeSelect = ref(1)
const isAgreed = ref(false)
const isAgreed01 = ref(0)
const selectedOS = ref([])
const selectedOSSelect = ref([])
</script>
